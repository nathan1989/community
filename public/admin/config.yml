backend:
  name: git-gateway
  branch: v2 # Branch to update (optional; defaults to master)

media_folder: "public/uploads" # Where media files will be stored
public_folder: "/uploads" # Where the media files can be accesed from the server
publish_mode: editorial_workflow # For Drafts

collections:
  - name: "pages"
    label: "Pages"
    files:
      - label: "Home"
        name: "home"
        file: "content/pages/home.md"
        preview_path: "/"
        fields:
          - {label: "Day", name: "day", widget: "string"}
          - {label: "Times", name: "times", widget: "string"}
          - {label: "Location", name: "location", widget: "string"}
      - label: "About"
        name: "about"
        file: "content/pages/about.md"
        preview_path: "/about"
        fields:
          - {label: "Title", name: "title", widget: "string"}
          - {label: "Body", name: "body", widget: "markdown"}
      - label: "Playlists"
        name: "playlists"
        file: "content/pages/playlists.md"
        preview_path: "/playlists"
        fields:
          - {label: "Body", name: "body", widget: "markdown"}
      - label: "Lyrics"
        name: "lyrics"
        file: "content/pages/lyrics.md"
        preview_path: "/lyrics"
        fields:
          - {label: "Body", name: "body", widget: "markdown"}
      - label: "I have decided"
        name: "i-have-decided"
        file: "content/pages/i-have-decided.md"
        preview_path: "/i-have-decided"
        fields:
          - {label: "Title", name: "title", widget: "string"}
          - {label: "Body", name: "body", widget: "markdown"}
  - name: "playlist" # Used in routes, e.g., /admin/collections/playlist
    label: "Playlist" # Used in the UI
    folder: "content/playlists" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    preview_path: "/playlists/{{slug}}" # The path to the preview page
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Subtitle", name: "subtitle", widget: "string"}
      - {label: "Song link", name: "link", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime", date_format: "DD.MM.YYYY", time_format: "HH:mm", format: "LLL"}
      - {label: "Lyrics", name: "body", widget: "markdown"}
  
  - name: "blog" # Used in routes, e.g., /admin/collections/blog
    label: "Blog" # Used in the UI
    folder: "content/blog" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    preview_path: "/blog/{{slug}}" # The path to the preview page
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Subtitle", name: "subtitle", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime", date_format: "DD.MM.YYYY", time_format: "HH:mm", format: "LLL"}
      - {label: "Content", name: "body", widget: "markdown"}