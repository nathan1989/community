export type Playlist = {
  slug: string,
  title: string,
  date: string,
  description: string,
  image: string,
  playlist: string,
  lyrics: string
}

export type PlaylistItem = {
  title: string
}
